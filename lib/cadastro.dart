import 'package:flutter/material.dart';

class CdastroView extends StatelessWidget {
  const CdastroView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cadastro'),
      ),
    );
  }
}
